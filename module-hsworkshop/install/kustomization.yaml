apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - rbac.yaml
  - postgres.yaml
  - redis.yaml
  - clickhouse.yaml
  - langfuse.yaml
  - openwebui.yaml
  # guardrails.yaml excluded - requires additional ClusterRole for trustyai.opendatahub.io CRDs
